import { ScoreScaleList } from '@/components/avaliacao/score-scale-list';

/** @deprecated Use ScoreScaleList — mantido para compatibilidade em telas legadas. */
export function EscalaLegenda() {
  return <ScoreScaleList />;
}
