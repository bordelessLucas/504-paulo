/**
 * @deprecated Use `NetworkStatusBar` no layout global `(main)/_layout.tsx`.
 * Mantido para telas que ainda importam localmente — evita barras duplicadas fora do shell.
 */
export { NetworkStatusBar as SyncStatusBar } from '@/components/ui/network-status-bar';
