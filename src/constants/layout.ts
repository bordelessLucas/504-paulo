import { Spacing } from '@/constants/theme';

/** Altura base dos ícones + label da tab bar (sem home indicator). */
export const TAB_BAR_BASE_HEIGHT = 50;

/** Folga extra entre o fim do conteúdo e a tab bar. */
export const TAB_CONTENT_EXTRA_PADDING = Spacing.three;

/** Folga para toasts e footers fixos acima da tab bar. */
export const TAB_FOOTER_EXTRA_PADDING = Spacing.two;
